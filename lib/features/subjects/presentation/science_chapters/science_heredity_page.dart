import 'package:flutter/material.dart';

import 'science_chapter_base.dart';

class ScienceHeredityPage extends StatelessWidget {
  const ScienceHeredityPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const ScienceChapterBase(
      chapterId: 'science_heredity',
      chapterTitle: 'Heredity',
    );
  }
}
