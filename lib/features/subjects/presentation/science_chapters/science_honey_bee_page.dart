import 'package:flutter/material.dart';

import 'science_chapter_base.dart';

class ScienceHoneyBeePage extends StatelessWidget {
  const ScienceHoneyBeePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const ScienceChapterBase(
      chapterId: 'science_honey_bee',
      chapterTitle: 'Honey Bee',
    );
  }
}
